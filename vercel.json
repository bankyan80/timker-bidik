{
  "version": 2,
  "builds": [
    {
      "src": "dist/app.cjs",
      "use": "@vercel/node",
      "config": { "includeFiles": ["dist/**"] }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/dist/app.cjs" },
    { "src": "/(.*)", "dest": "/dist/app.cjs" }
  ]
}
